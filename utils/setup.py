from setuptools import setup, find_packages

setup(
    name='bird_utils',
    version='0.2',
    packages=find_packages(),
    author='Gavin Straub',
    author_email='straub.gavin@gmail.com',
    description='Utilites for bird classification algorithm',
    install_requires=[
        # add any dependencies your package requires
    ],
)